import { PageStub } from "@/components/page-stub";

export default function NotFound() {
  return (
    <PageStub
      eyebrow="404"
      title="This path doesn't exist."
      line="The page you're looking for isn't here. The way forward is back to the start."
      sprint="—"
    />
  );
}
