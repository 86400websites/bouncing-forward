import { PageStub } from "@/components/page-stub";

export default function HomePage() {
  return (
    <PageStub
      eyebrow="Turn the page"
      title="Not Back. Forward."
      line="Loss isn't the finish line. It's the forest you have to cross — and on the other side is the person you're capable of becoming."
      sprint="sprint 3"
    />
  );
}
