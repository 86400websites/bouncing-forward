Delete these two folders (the zip overlay cannot remove them); their
routes now live as redirects in next.config.ts:

  src/app/learn
  src/app/workshops

PowerShell, from the project root:
  Remove-Item -Recurse -Force "src/app/learn", "src/app/workshops"

Then rename the three compass icons per RENAME-THESE.txt, run
pnpm run format:check && pnpm run build (both green), and ship via
branch → PR → Code Check → merge.
